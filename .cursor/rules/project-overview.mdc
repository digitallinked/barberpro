---
description: BarberPro project overview, monorepo structure, and context for all AI assistance
alwaysApply: true
---

# BarberPro Project Context

BarberPro is a **Malaysia-first, multi-tenant barber shop management SaaS** built as a pnpm monorepo.

## Monorepo Apps

| App | Package | Domain | Status |
|---|---|---|---|
| `apps/web` | `@barberpro/web` | `shop.barberpro.my` | Active — barber shop owners & staff |
| `apps/web-admin` | `@barberpro/web-admin` | `admin-pro.barberpro.my` | Active — BarberPro super admin |
| `apps/customer` | `@barberpro/customer` | `barberpro.my` | Planned Phase 2 — end customers |
| `apps/mobile-customer` | `@barberpro/mobile-customer` | App Store / Play Store | Planned Phase 4 |
| `apps/mobile-staff` | `@barberpro/mobile-staff` | App Store / Play Store | Planned Phase 5 |

## Shared Packages

| Package | Purpose | Status |
|---|---|---|
| `packages/db` | Supabase clients + shared queries | Planned (extract from apps/web) |
| `packages/auth` | Auth helpers, getAuthContext | Planned (extract from apps/web) |
| `packages/ui` | shadcn/ui design tokens + shared web components | Exists |
| `packages/ui-native` | NativeWind React Native components | Planned |
| `packages/types` | Shared TypeScript types | Exists (placeholder) |
| `packages/config` | Shared tsconfig base | Exists |
| `packages/env` | Typed env helpers | Exists |
| `packages/notifications` | Expo push notification helpers | Planned |

## Backend

- **Supabase** — single project, PostgreSQL, RLS for tenant isolation
- **Stripe** — subscription billing for barber shop operators
- **Vercel** — deployment platform, separate project per web app
- **Expo EAS** — mobile app builds and OTA updates

## Current Active Work (Phase 0)
Before adding features, fix these production correctness issues:
1. Stripe webhook must use `createAdminClient()` not `createClient()` — no session in webhook
2. Queue board RLS `using (true)` exposes all tenants' data — must scope to active branches
3. Middleware makes 1–3 DB queries per request — cache tenant state in cookie post-login
4. Add Sentry error monitoring
5. Add Resend for transactional email
6. Set up GitHub Actions CI

## Key Documentation
- `docs/ARCHITECTURE.md` — full system architecture
- `docs/ROADMAP.md` — phased delivery plan
- `docs/PRODUCT_BRIEF.md` — product vision and user personas
- `docs/DATABASE_SCHEMA.md` — all tables, columns, relationships
- `docs/SECURITY.md` — security rules (read before any auth/DB work)
- `docs/CONTRIBUTING.md` — coding standards and monorepo workflow
- `docs/DEPLOYMENT.md` — Vercel, CI/CD, migrations
- `docs/MOBILE_STRATEGY.md` — Expo apps plan

## Language & Pricing Context
- Primary market: Malaysia
- Currency: Malaysian Ringgit (MYR / RM)
- Languages: Bahasa Malaysia (ms), English (en), Mandarin (zh)
- i18n is already integrated in `apps/web` via `LanguageProvider`
