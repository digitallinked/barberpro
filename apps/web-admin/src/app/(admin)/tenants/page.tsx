import { PagePlaceholder } from "@/components/page-placeholder";

export default function TenantsPage() {
  return (
    <PagePlaceholder
      title="Tenants"
      description="Manage tenant lifecycle, plan assignment, and account status."
    />
  );
}
