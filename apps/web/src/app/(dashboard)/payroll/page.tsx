import { PagePlaceholder } from "@/components/page-placeholder";

export default function PayrollPage() {
  return (
    <PagePlaceholder
      title="Payroll"
      description="Review payout periods, approvals, deductions, and transfer status."
    />
  );
}
