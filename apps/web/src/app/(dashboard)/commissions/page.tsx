import { PagePlaceholder } from "@/components/page-placeholder";

export default function CommissionsPage() {
  return (
    <PagePlaceholder
      title="Commissions"
      description="Configure fixed, per-service, percentage, and hybrid commission rules."
    />
  );
}
