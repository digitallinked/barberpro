import { PagePlaceholder } from "@/components/page-placeholder";

export default function AppointmentsPage() {
  return (
    <PagePlaceholder
      title="Appointments"
      description="Handle booking schedules, no-shows, and barber availability."
    />
  );
}
