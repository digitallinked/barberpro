import { PagePlaceholder } from "@/components/page-placeholder";

export default function ExpensesPage() {
  return (
    <PagePlaceholder
      title="Expenses"
      description="Capture branch expenses, categories, and payment breakdown reporting."
    />
  );
}
